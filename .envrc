if nix flake show &> /dev/null; then
  echo "-----------------------------------------"
  echo "Launching DBC Deployment DevShell (flake)"
  echo "-----------------------------------------"
  use flake .
else
  echo "---------------------------------------------"
  echo "Launching DBC Deployment DevShell (nix-shell)"
  echo "---------------------------------------------"
  use nix
fi
